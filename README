BITS.UTIL LIBRARY

Various utilities. I developed this primarily as a personal toolkit,
but many other projects depend on it.


BUILDING:
1. ant


RUNTIME:
1. All files in "lib" and "dist" directories (after build) are required in classpath. 


AUTHOR:
Philip DeCamp